{
  "version": 2,
  "routes": [
    { "src": "/robots.txt", "dest": "/robots.txt" },
    { "src": "/sitemap.xml", "dest": "/sitemap.xml" },
    { "src": "/manifest.json", "dest": "/manifest.json" }
  ],
  "functions": {
    "app/**": { "maxDuration": 10 }
  }
}
